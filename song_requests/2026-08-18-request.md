# 2026-08-18 曲目请求交接

## 已入库候选：肘我（江湖梦二创）

更正：用户请求中的“肘我”不是 `builtin_songs/恕我.txt`，不得与《恕我》合并或作为别名覆盖。当前已按独立曲目 `肘我（江湖梦二创）` 入库候选，判断依据：

- 来源证据：`https://b23.tv/ZIhX6dQ`（BV1feFAzdEn2）。
- 本地输出：`music_player_next/builtin_songs/肘我.txt`，550 事件，推荐 556 ms/拍；括号内容仅保留在来源证据中。
- 精修交接：`source_audio/肘我_GAME/肘我_精修日志.md` 明确写明本曲保持独立标题，不得与《恕我》合并。
- 状态：`audio-derived candidate`，已入三端曲库，仍需游戏内试听校准后才能标记 final。

运行时展示名称统一为 `肘我`；不要把搜索结果重定向到《恕我》。来源仍是该 B 站二创，不与《恕我》合并。

## 已入库候选：朋友的酒

版本：李晓杰《朋友的酒》；本轮采用 B 站完整 DJ 小鱼儿版 `BV1h1421m7QY`（6:20）作为可核验音源。已保存 `source_audio/朋友的酒_BV1h1421m7QY.m4a/.wav`，Basic Pitch 输出 `source_audio/朋友的酒_GAME/朋友的酒_BV1h1421m7QY_basic_pitch.csv/.mid`，并生成 `music_player_next/builtin_songs/朋友的酒.txt`（1248 events，推荐 465 ms/拍）。

状态：`audio-derived candidate; requires in-game audition`。该版本为 DJ 编曲，自动转写可能含伴奏误检，需游戏内试听后再标记 final。

## 已入库候选：归零

状态：`audio-derived candidate`。当前选用魔鬼花园版本 `BV1nN411879y`，已生成 `music_player_next/builtin_songs/归零.txt`，600 事件，推荐 534 ms/拍，并同步到 Windows 老版、新版和 Android 三端 167 首曲库。

可复现下一步：游戏内试听，重点检查伴奏误检、错音和重叠音顺序化问题；若用户要其他同名版本，应另起独立任务。

## 已入库候选：偶像 / アイドル

状态：`audio-derived candidate`。当前选用 YOASOBI 版本 `BV1zw411w7Dw`，已生成 `music_player_next/builtin_songs/偶像.txt`，783 事件，推荐 449 ms/拍，并同步到 Windows 老版、新版和 Android 三端 167 首曲库。

可复现下一步：游戏内试听，重点检查伴奏误检、错音和节奏密度；尚未标记 final。

## 桌面端 -> Android 曲库交接

桌面端的可发布曲库真值是 `music_player_next/builtin_songs/*.txt`，不是 `build/` 或 `dist/` 缓存。同步 Android 时应复制这些 TXT（保留 UTF-8 文件名和 `# 推荐节拍` 注释），由 Android 首次启动导入/解析；新增曲目以后以该目录为来源，避免手工维护两份列表。

## 周杰伦批次并发收尾（2026-08-18）

- Super mode 使用 3 个 d1 worker 并行处理用户指定的 22 首；已完成 19 首并生成 TXT、来源报告及音频转谱素材，统一状态为 `audio-derived candidate; requires in-game audition`。
- 已入库 19 首：简单爱、龙卷风、安静、搁浅、彩虹、珊瑚海、一路向北、花海、爱在西元前、东风破、蒲公英的约定、听妈妈的话、给我一首歌的时间、说好的幸福呢、霍元甲、等你下课、说好不哭、Mojito、最伟大的作品。
- 阻塞但已留证 3 首：轨迹、半岛铁盒、菊花台。官方/公开来源受到 YouTube 反爬、网易云版权页或无可靠原曲素材限制，未生成伪造 TXT；对应 `source_audio/*_GAME` 或 `source_audio/周杰伦_*_报告.json` 记录了尝试来源和原因。
- 桌面曲库从 176 增至 195 首；下一步只需运行跨平台检查、Android 测试/构建并更新 APK，不要重跑已完成歌曲。
